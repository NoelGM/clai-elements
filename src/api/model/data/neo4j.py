def get_patient_params(uri: str, user: str, password: str, identifier: str, topic: str):

    return {
        "uri": uri,
        "user": user,
        "password": password,
        "identifier": identifier,
        "topic": topic
    }
